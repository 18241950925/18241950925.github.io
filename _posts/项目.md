### TODO
1. 实习项目：优先吃透算子与布局、正确性、内存与搬运隐藏三条主线
2. 算子优化：Roofline、tiling、数据布局、访存合并、向量化、融合、双缓冲与性能验证
3. 编译器：LLVM/MLIR Pass、算子图 lowering、内存规划、调度与后端代码生成
4. 国产卡训练：混合精度、反向传播、并行策略、集合通信、显存估算与性能诊断
5. 多模态基模：Transformer/DiT、视频 VAE、时空注意力、训练与推理差异
6. CUDA/Triton：复习 SNN CUDA 代码，并各写一个可 profile 的算子优化项目
7. PyTorch：能够从模型定义、训练循环、AMP 到 DDP/FSDP 跑通一套最小训练工程

## 实习项目

### 面试定位

这段实习的核心不是“调用 NPU 跑模型”，而是围绕 **模型计算图—后端调度—量化权重布局—矩阵/向量计算单元—内存生命周期—端到端性能与正确性** 做完整链路优化。基于最新项目总述，面试内容应收敛为三条主线：

1. **算子与数据布局**：DeepSeek MoE Q4 权重预打包，以及 Flash Attention 分块成本函数优化。前者减少运行时解量化和布局转换，后者改善分块调度、降低硬件空闲。
2. **算子正确性**：HMX Flash Attention mask 修复。体现对算子契约、数值稳定性、差分测试和回归验证的理解。
3. **内存与搬运优化**：layerwise 权重加载解决模型容量问题，跨层 Expert 预测预取进一步用计算覆盖权重搬运延迟。

GPT-OSS MoE dispatch 和 NPU Prefill + CPU Decode 作为补充案例：前者用于回答硬件利用率、Roofline 和调度粒度，后者用于回答异构执行、KV cache 与资源生命周期。

需要主动说明经验边界：实习硬件是 Qualcomm Snapdragon SM8850 的 Hexagon HTP v81，属于移动端 NPU，不是国产训练卡；工作重心也是 **推理后端**，不是大规模训练。但数据布局、算子映射、异构调度、profiling、数值验证等方法可以迁移到国产卡训推。训练相关内容应表述为正在系统补齐，不能把推理经验包装成训练落地经验。

### 90 秒项目总述

> 我的实习主要做 Snapdragon SM8850 / Hexagon HTP 上的端侧大模型推理优化，基于 `llama.cpp` 打通从 GGUF 量化权重、后端算子、MoE 路由与 expert staging，到 Android 部署和性能验证的完整链路。我的工作可以分成三类：第一类是算子和数据布局，例如将 DeepSeek MoE 的 Q4 Gate/Up 权重预先打包成硬件友好的布局格式，以及优化flashattention的分块成本函数，降低硬件的空闲时间；第二类是正确性，定位并修复 HMX Flash Attention 把有限负 bias 误判成 mask 的问题，使完整回归 2176/2176 通过；第三类是内存优化，通过layerwise加载让大规模的模型可以部署到端侧，并通过expert预测来用计算掩盖搬运延迟。

面试时不要一口气讲完所有成果。说完总述后停下来，让面试官选择深挖方向；若让自己选，优先进入 DeepSeek MoE 权重布局项目。

### 技术链路总图

`模型/GGUF` → `量化权重与离线打包` → `layerwise / expert staging` → `llama.cpp 计算图` → `Hexagon 后端算子、分块与 graph dispatch` → `Q6 调度 + HVX 向量计算 + HMX 矩阵计算 + DMA/VTCM` → `logits/PPL/真实生成正确性` → `端到端 TPS、trace 与硬件利用率`

需要能解释每一层：

- `llama.cpp` 前端构造计算图，后端根据算子支持情况和 buffer 所在设备选择执行路径。
- Host 侧负责模型加载、GGUF/Q4 权重处理、layerwise/expert staging、任务提交和部分 fallback；HTP 侧执行面向 HVX/HMX 优化的算子。
- HMX 适合矩阵乘，HVX 适合向量化处理，Q6 还承担控制与调度；VTCM 是容量有限但延迟和带宽更有优势的片上存储，DMA 用于搬运。
- layerwise 加载解决的是“模型能否放下”，Expert 预取解决的是“按需加载是否进入关键路径”；两者组合后还要处理峰值内存、预取命中率和计算—搬运重叠。
- 性能不能只看“NPU 总耗时”，还要区分计算单元利用率、空闲时间、搬运、图调用、控制核调度、CPU fallback 和异步重叠。

### 主项目一：DeepSeek MoE Gate/Up Q4 权重布局优化

#### 一句话版本

> 通过 profiling 发现 Q4 MoE Gate/Up 热路径包含 host repack、DSP Q4 decode 和 HMX FP16 tile transform；在排除 loop reorder 后，我把权重提前打包成 HMX-friendly 的 x4 K-tile 布局，以离线空间和兼容分支换取更少的运行时转换，使局部 Gate/Up 降低 41.6%、pp1024 端到端 prefill 提升 10.67%，并保持 PPL 一致。

#### 问题与诊断

- MoE 每层只激活部分 expert，但命中的 expert 权重仍需进入 Gate/Up fused matmul 路径。
- 原路径为：`Q4_0 原始布局 → host repack → DSP 解码 → HMX FP16 tile-major 转换 → HMX matmul`。
- profile 显示 dequant/layout transform 是 Gate/Up 的主要热区，因此问题不仅是矩阵乘本身，而是“为了让矩阵单元吃到合适布局，运行时付出了多少准备成本”。
- 曾假设通过交换 M/N 循环顺序复用权重；但路由统计显示单 expert 的 `m_chunks` 基本为 1，没有足够的 M 维复用，重排还增加 activation gather，因此用数据否定该方案。

#### 优化设计

- 将重复且与输入无关的布局工作前移到 host/离线阶段，直接生成 HMX 需要的 packed Q4 tile。
- fast path 一次处理连续 4 个 K tile，降低循环、地址计算和重复转换开销，并保留批处理优势。
- 保留默认兼容路径，用环境变量选择实验路径，便于做严格 A/B、快速回退和控制正确性风险。
- 本质是用更多的离线准备、存储布局约束和实现复杂度，换取热路径更低的 dequant/repack 成本。

#### 为什么这属于算子/编译优化问题

- **数据布局决定可生成的高效代码**：逻辑上相同的权重，物理排列不同，会改变连续访存、向量 load、tile 填充和矩阵单元喂数效率。
- **把运行时不变量前移**：权重布局只依赖模型参数和目标硬件，可视为 ahead-of-time packing；这和编译器的 constant folding、layout propagation、weight prepacking 思路一致。
- **需要合法性与收益判断**：只有 dtype、shape、对齐和硬件路径满足条件才进入 fast path，否则回退；工程上相当于带 guard 的 specialization。
- **局部提速不等于端到端提速**：必须继续检查 attention、路由、其他 expert、CPU 提交等剩余瓶颈。

#### 指标与验证

- pp1024：`641.06 ± 17.98 t/s → 709.46 ± 17.30 t/s`，端到端提升 **10.67%**。
- Gate/Up：`12335 μs → 7200 μs`，降低 **41.6%（1.71x）**。
- Dequant：`8461 μs → 3225 μs`，降低 **61.9%（2.62x）**。
- 正确性：PPL 保持一致；默认路径不受影响。

#### 高频追问

**Q：为什么不直接优化 HMX matmul？**

A：profile 指向的主要开销是进入 matmul 前的解量化和布局转换。如果矩阵单元没被持续喂饱，单独提高 matmul 峰值不会等比例改善端到端。应先优化占比最大且可以消除的准备工作，再用新 profile 判断下一瓶颈。

**Q：为什么 x4 K-tile 会更快？**

A：核心不是数字 4 本身，而是批量处理相邻 K tile 后，可以摊薄循环控制、索引计算和转换开销，并形成更连续的 load/store。最终是否有效取决于 tile 尺寸、向量宽度、对齐、VTCM/寄存器压力和真实 shape，需要 benchmark 而不能只凭直觉。

**Q：预打包的代价是什么？**

A：会增加模型准备时间、格式维护成本，可能带来 padding 或额外存储，也会使格式与特定硬件耦合；动态权重或 shape 不满足 guard 时收益有限。因此要保留通用路径，并把格式版本、对齐条件和兼容性纳入设计。

**Q：如果迁移到国产训练卡，思路如何复用？**

A：先确认矩阵单元支持的 dtype、tile 和 layout，再检查框架图中是否反复出现 transpose/cast/reformat。对静态参数可做离线 prepack，对 activation 则通过 layout propagation、算子融合或 producer-consumer 协同减少转换。训练场景还要处理权重更新：master weight 和 optimizer state 会变化，不能简单永久预打包，需要在参数更新后重排、使用硬件原生布局保存参数，或把布局转换融合进优化器。

#### 面试前必须从代码补齐的事实

- `[待确认]` Q4_0 block 的实际字节布局、scale 类型、每 block 元素数。
- `[待确认]` HMX tile 的具体 M/N/K 尺寸、x4 打包的循环结构与地址公式。
- `[待确认]` fast path 的 shape、对齐、dtype guard 和 fallback 条件。
- `[待确认]` 环境变量名、关键函数名、修改文件、benchmark 命令和 PPL 容差。
- 至少能在纸上画出一个小矩阵从原始 Q4 布局到 HMX tile-major 布局的索引映射。

### 算子项目二：Flash Attention 分块成本函数优化

#### 一句话版本

> Flash Attention 的分块不仅决定单块计算效率，也决定任务能否均匀覆盖硬件执行资源。我针对原有分块成本函数不能准确反映实际执行代价的问题，调整候选分块的评价方式，使调度更倾向于空闲时间更少的方案，从而改善整体硬件利用率。

目前总述只记录了优化方向，没有保留成本公式、调度对象和量化结果。面试前必须回到代码和实验记录补全，否则只能作为第二项目简述，不能主动进入公式级深挖。

#### 这项工作的核心矛盾

- tile 太小：单块工作量不足，调度和循环控制开销占比高，也可能无法高效使用矩阵单元。
- tile 太大：并行任务数量减少，尾部 block 或不同计算单元之间容易负载不均，还会增加片上存储和寄存器压力。
- 只最小化单个 tile 的理论计算量并不够；成本函数还需要反映总 tile 数、可并发任务数、尾块、padding、访存和硬件资源占用。
- 因此目标不是寻找“计算量最小的 tile”，而是寻找 **端到端执行时间更小、硬件空闲更少且资源约束合法** 的分块方案。

#### 面试讲述框架

1. **原始策略**：说明候选 tile 如何产生，旧成本函数包含哪些项、忽略了哪些硬件行为。
2. **问题证据**：通过 timeline、硬件计数器或不同 shape 的 benchmark，证明存在负载不均或执行资源空闲，而不是笼统地说“利用率低”。
3. **成本模型**：写出旧公式和新公式，解释每一项的单位、权重、约束以及为什么能近似真实执行时间。
4. **调度结果**：举一个具体 shape，对比优化前后选择的 tile、任务数量、尾块和预计空闲时间。
5. **验证闭环**：分别给出算子耗时、硬件利用率、端到端吞吐和正确性结果。

#### 高频追问

**Q：为什么需要成本函数，不能枚举所有 tile 后实测吗？**

A：离线 autotuning 可以实测，但搜索成本高，且模型运行时可能出现大量 shape。成本函数能够低成本筛选候选，再对少量候选实测；如果 shape 固定，也可以把实测最优结果缓存。工程上通常是启发式模型、约束剪枝和 autotuning 结合。

**Q：怎样定义“硬件空闲时间”？**

A：不能只给口头定义。需要明确它是由总工作块数不能均匀覆盖并行执行资源产生的理论 idle slot，还是从 timeline/计数器测得的执行单元空闲周期。面试时应同时给出计算方法和测量证据，避免把模型估计值说成硬件实测值。

**Q：成本模型为什么可能在另一个 shape 上失效？**

A：不同 shape 下，尾块比例、计算访存比、并行任务数和片上资源压力都会变化；启发式权重也可能只适配当前硬件。因此要覆盖短序列、长序列、不同 head dimension 和 causal/non-causal 场景，并保留合法回退策略。

#### 面试前必须从代码补齐的事实

- `[待确认]` 被分块的是 QK、softmax、PV 的哪一层循环，调度单位是 tile、thread、vector 还是 HMX job。
- `[待确认]` 旧成本函数和新成本函数的完整公式、每个变量的物理含义与单位。
- `[待确认]` “硬件空闲时间”的计算方式、观测工具以及优化前后数据。
- `[待确认]` 典型 Q/K/V shape、候选 tile、片上内存限制和合法性条件。
- `[待确认]` 算子与端到端性能提升、测试次数、误差容限和覆盖的边界 shape。

### 正确性项目：HMX Flash Attention mask 修复

#### 一句话版本

> HMX 路径把所有小于 `-16` 的 FP16 mask 值都当成不可见位置，误删了合法的有限负 attention bias；我通过关闭 HMX 的参考路径和 focused test 将问题定位到 mask 语义，修改为仅屏蔽真正的 FP16 `-inf`，使 focused test 从 12/56 提升到 56/56、完整回归达到 2176/2176，并恢复真实模型生成。

#### 必须讲清的数学语义

Attention 的核心是：

`S = QK^T / sqrt(d) + mask_or_bias`

`P_i = exp(S_i - max(S)) / Σ_j exp(S_j - max(S))`

有限负 bias 表示“降低该位置概率”，它仍应进入 softmax；`-inf` 才表示该位置概率严格为 0。用固定阈值把小于 `-16` 的数全部丢弃，会把连续的 bias 语义错误地变成布尔 mask 语义，改变输出分布。

#### 定位路径

1. 相同输入下，关闭 HMX 全通过、开启 HMX 大量失败，先把范围缩到 HMX fast path。
2. focused case 的最大误差接近 `0.98`，不像普通 FP16 累积误差，更像分支或语义错误。
3. 对比 mask/bias 处理，发现阈值判断错误地屏蔽有限负值。
4. 修复后依次做 focused test、完整算子回归、真实 OLMoE 生成，避免只在单一 fixture 上过拟合。

#### 指标与验证

- focused test：`12/56 → 56/56`。
- 完整 `FLASH_ATTN_EXT`：`2176/2176`，`FAIL=0`。
- 真实生成恢复正常；开启 Flash Attention 后 prompt 吞吐 `64.6 → 71.2 t/s`。

#### 高频追问

**Q：为什么 softmax 要减去最大值？**

A：不改变最终概率，因为分子分母同时除以 `exp(max)`；但能防止大正数 `exp` 上溢。低精度下还要关注累加精度、online softmax 的 running max/running sum 更新和全 mask 行等边界情况。

**Q：如何区分算法误差和实现 bug？**

A：先用可靠路径做 reference，固定输入比较逐元素误差；再看误差量级、分布和出现条件。微小且随归约长度增长的偏差更像舍入/累积误差，接近 1 的结构性偏差更像索引、mask、layout 或同步问题。随后用最小 shape、特殊 mask、全 mask/无 mask/有限 bias 等用例缩小范围。

**Q：为什么不能为了性能近似处理 mask？**

A：近似必须符合上层算子的契约，并有明确误差界和调用方约束。这里输入允许有限负 bias，因此阈值化改变了语义，不能作为合法近似。若专门为纯 causal boolean mask 写 fast path，应通过 dtype/模式 guard 区分。

#### 面试前必须补齐

- `[待确认]` 原判断与修复后判断的准确代码，是否用 bit pattern、`isinf` 或常量比较。
- `[待确认]` 56 个 focused case 和 2176 个完整 case 覆盖了哪些 dtype、shape、mask 与 causal 组合。
- `[待确认]` reference 路径、误差指标、绝对/相对容差和真实生成命令。
- 复习 Flash Attention 的 IO-aware tiling、online softmax 合并公式、causal mask 和 KV cache 场景。

### 内存项目一：Layerwise 权重加载

#### 一句话版本

> 端侧内存无法同时容纳大规模模型的全部 NPU 权重与执行上下文，因此我将原来的整体常驻方式改为 layerwise 加载：执行到某层时只准备该层需要的权重，完成后释放或复用设备侧 buffer，用更小的峰值驻留集合换取模型可部署性；代价是权重搬运可能进入关键路径，因此后续又结合 Expert 预测预取隐藏部分加载延迟。

#### 解决的问题

- 模型文件能够 mmap 到 CPU 地址空间，不代表 HTP 可以零成本、无限容量地直接访问全部权重。
- 端到端内存需要同时考虑模型权重、量化/重排后的副本、HTP 映射 buffer、KV cache、compute workspace 和 graph context。
- 如果全部权重或大量 expert context 常驻设备侧，可能在模型加载或长上下文推理前就达到内存上限。
- layerwise 的核心是缩小同一时刻必须驻留的 working set，使峰值内存近似由“全模型设备权重”下降到“当前层/少数预取层权重 + 常驻状态”。

#### 需要讲清的执行流程

`确定下一执行层` → `准备/映射该层权重` → `必要的反量化或布局转换` → `提交该层计算` → `等待生命周期安全点` → `释放或复用 buffer` → `进入下一层`

需要区分三件事：

- **磁盘到 Host**：文件读取、mmap/page fault 和 page cache。
- **Host 到 HTP 可访问内存**：rpcmem/FastRPC 映射、拷贝或注册成本。
- **进入算子所需布局**：量化权重 repack、dequant 和 HMX/HVX 需要的格式转换。

layerwise 只说明按层管理生命周期；每一步究竟是 copy、map 还是 zero-copy，必须以实际实现为准。

#### 关键权衡

- **峰值内存 vs. 搬运延迟**：驻留越少越省内存，但重复加载越可能拖慢每层执行。
- **预取距离 vs. 峰值内存**：提前一层或多层可增加重叠窗口，但会扩大同时驻留集合。
- **双缓冲 vs. 额外空间**：两组 buffer 可让当前层计算和下一层加载并行，但至少需要容纳两个工作集。
- **通用层与 MoE expert**：dense 层访问确定，适合顺序预取；MoE expert 由 router 动态选择，需要预测或先路由后加载。
- **KV cache**：其大小随序列长度增长且不能像普通中间 tensor 一样逐层永久释放，必须与权重 residency 联合规划。

#### 高频追问

**Q：为什么 mmap 不能直接解决 NPU 权重容量问题？**

A：mmap 解决的是文件到 CPU 虚拟地址空间的映射和按页加载，不等价于权重已经处于 HTP 算子可直接、高效访问的内存与目标布局。后端仍可能需要 rpcmem/FastRPC 映射、设备侧 buffer、repack 或 staging，这些都会占用额外内存并产生延迟。

**Q：怎样保证释放权重时没有异步任务仍在使用？**

A：必须依据执行流中的 event/fence 或同步点管理生命周期。Host 提交完成不代表设备执行完成；只有依赖该 buffer 的任务确认结束后，才能释放或复用。更稳妥的实现会把 buffer 与 stream/event 或层级状态机绑定。

**Q：layerwise 为什么可能明显降低吞吐？**

A：如果每层加载完全串行地落在关键路径上，总时间会接近 `Σ(load_i + compute_i)`。理想流水是 `load_{i+1}` 与 `compute_i` 重叠，使稳定阶段接近 `Σ max(load_{i+1}, compute_i)`，但前提是 DMA 与计算可并行、带宽足够且有双缓冲空间。

#### 面试前必须从代码补齐的事实

- `[待确认]` 哪些 tensor 永久驻留，哪些按层加载，MoE expert 是否单独管理。
- `[待确认]` 修改前后的峰值 RSS、rpcmem/HTP 内存和可部署模型规模。
- `[待确认]` 每层 load、map、repack、compute 的 timeline，是否使用双缓冲和异步 DMA。
- `[待确认]` buffer 的申请、复用、同步与释放位置，以及异常路径如何清理。
- `[待确认]` layerwise 对 prefill/decode TPS、首 token 时延和 p95/p99 token latency 的影响。

### 内存项目二：跨层 MoE Expert 预测预取

#### 核心逻辑

- 当前层 hidden state 与下一层 router top-K 存在统计相关性，可在当前层计算期间预测并 staging 下一层候选 expert 权重。
- 预测预取是 layerwise/按需加载之后的延迟优化：layerwise 降低峰值驻留，预测器则争取把不可避免的搬运移出关键路径。
- top-K 太小会 miss，太大会造成无效搬运和内存占用；因此需要联合评估 offline recall、候选精度、加载字节数和在线 `tg1` 吞吐。
- top-6 配置将单 token decode 从 `0.705 t/s` 提升到 `1.345 t/s`，提升 **90.8%**。
- 两阶段预测器的 Top-6 recall 为 `69.98%`，说明不需要 100% 命中也可能获得收益：只要命中的预取覆盖了关键路径的一部分，且错误预取代价受控即可。

#### 计算掩盖搬运的条件

设当前层可用于预取的计算窗口为 `T_compute`，候选权重准备时间为 `T_load`：

- 若 `T_load ≤ T_compute`，命中的候选理论上可以被完全隐藏。
- 若 `T_load > T_compute`，关键路径仍增加约 `T_load - T_compute`。
- miss 时还要额外承担按需加载；错误预取则浪费带宽和 buffer，甚至可能挤占真正需要的 expert。

因此最终收益不只由 recall 决定，还取决于命中 expert 的加载时长、预取提前量、错误预取代价、并发 DMA 能力和内存 headroom。

#### 高频追问

**Q：为什么 Top-6 recall 只有约 70%，吞吐仍能提升 90.8%？**

A：recall 与吞吐不是线性关系。只要命中的预取覆盖了原关键路径中代价较高的部分，而 miss 有正确 fallback、错误预取又能与计算并行，系统就可能获得显著收益。还需要说明 `tg1` 是特定单 token 场景，不能将 90.8% 泛化到所有序列长度。

**Q：预测错误会不会改变模型输出？**

A：不会。预测器只决定提前搬运哪些候选；真实 router 输出仍决定实际执行的 expert。miss 时走按需加载 fallback，因此预测影响时延和带宽，不改变 MoE 数学语义。

**Q：怎样选择预取 top-K？**

A：通过 sweep 联合比较 recall、候选精度、额外搬运字节、峰值内存和在线 decode 吞吐。top-K 增大通常提高 recall，但边际收益递减，同时无效搬运和内存压力上升，因此不能只按离线准确率选配置。

#### 面试前必须从代码补齐的事实

- `[待确认]` predictor 的输入位置、rank-16 结构、47 个预测槽位与 MPEP v2 格式字段。
- `[待确认]` 预测、DMA/staging、当前层计算和真实 router 的准确时序。
- `[待确认]` top-6 的平均加载字节数、命中时节省时延、miss fallback 成本和峰值内存。
- `[待确认]` `tg1` baseline/优化测试条件、重复次数、设备状态和更长 decode 的收益。

### 补充项目：GPT-OSS MoE dispatch 粒度与 capacity 优化

#### 一句话版本

> profile 显示 NPU 时间占比很高，但 HMX 平均利用率只有 1.3%、HVX 18.4%，Q6 控制核却满频，说明瓶颈主要是权重搬运、频繁 graph dispatch 和 slot 利用率，而不是矩阵峰值算力；我保持 attention chunk=128 以控制内存，同时把 MoE dispatch 合并到 512 token 并配套扩大 expert capacity，使 graph call 从 384 降到 96、pp2048 prefill 提升 10.3%。

#### Roofline 判断

- 算术强度 `AI = FLOPs / bytes moved`。
- 可达到的性能上界 `P ≤ min(P_peak, AI × BW)`。
- 该场景估算 AI 约 `248 FLOPs/byte`，低于 HTP Roofline 拐点约 `800 FLOPs/byte`，优先判断为 memory/dispatch-bound，而非 compute-bound。
- Roofline 是方向判断，不是最终证据；还需结合 HMX/HVX/Q6 利用率、DMA、graph call、CPU fallback 和 timeline。

#### 为什么解耦 attention 与 MoE chunk

- attention 和 MoE 的资源约束不同，不应机械共用同一 chunk。
- attention 增大 chunk 会增加中间状态和 CDSP 内存压力，因此保持 128。
- MoE 合并到 512 token 后，一次 dispatch 可聚合更多路由 token，提高单次 expert 权重复用并摊薄提交/调度成本。
- capacity 决定每个 expert graph 可容纳的 token 数；只增 dispatch 不增 capacity 会产生 overflow 或 CPU fallback，因此两者需要协同调整。

#### 指标与代价

- NPU graph calls：`384 → 96`，减少 **4 倍**。
- NPU expert 执行：`17.65 s → 13.80 s`，提升 **1.28x**。
- pp2048：`89.9 → 99.2 tok/s`，端到端提升 **10.3%**。
- 代价是更大的 expert context/capacity、可能增加内存占用和排队时延；最优 chunk 会随模型、序列长度、路由分布和设备内存变化。

#### 高频追问

**Q：NPU busy 很高，为什么利用率仍然低？**

A：busy 只表示设备在处理任务，不表示矩阵单元持续做有效 MAC。控制核调度、DMA 等待、layout 转换、小 shape、同步和 fallback 都可能计入时间。必须把设备占用和具体执行单元利用率分开看。

**Q：为什么 graph call 减少 4 倍，端到端只提升 10.3%？**

A：优化只覆盖 MoE expert 子阶段，端到端还包含 attention、路由、host 提交、数据搬运和其他固定开销；同时更大 dispatch 也可能引入 padding、capacity 和内存成本。Amdahl 定律决定局部收益会被未优化部分稀释。

**Q：chunk 是否越大越好？**

A：不是。增大 chunk 可以摊薄 launch/dispatch、提高权重复用，但会增加工作区、padding、排队时延和片上存储压力，甚至触发 OOM。需要在固定场景下 sweep，并同时观察 TPS、峰值内存和 p95/p99 时延。

#### 面试前必须补齐

- `[待确认]` 384 次 graph call 的计算方式，以及 token/layer/expert group 之间的关系。
- `[待确认]` capacity 的定义、overflow 触发条件、CPU fallback 路径和 context 重编步骤。
- `[待确认]` HMX/HVX 利用率的采样周期、平均方式与误差来源。
- 自己重新推导 `248 FLOPs/byte` 和 `800 FLOPs/byte` 的计算过程，不能只背结论。

### 补充项目：NPU Prefill + CPU Decode 异构路径

#### 核心逻辑

- Prefill 有较大的 token 维度并行度，NPU 更容易发挥吞吐；decode 通常每步一个或少量 token，更容易受 launch、状态和权重搬运影响。
- 在 NPU 完成 prefill 后，将有效范围内的 KV cache 复制到 CPU context，然后释放 NPU prefill context/model，再进行 CPU prewarm 和 decode。
- 关键正确性约束包括层数、K/V 张量布局、token range、position、dtype、stride 和 context 元数据一致。
- 释放 NPU context/model 后，decode 从 `1.80 t/s` 恢复到 `20.64 t/s`；CPU prewarm 将 pp2048+tg2048 中 `>300 ms` token 次数从 50 降到 0，端到端 `15.67 → 17.21 t/s`。

#### 追问准备

- 为什么 NPU 资源常驻会拖慢 CPU：从共享内存带宽、功耗/频率预算、缓存压力、线程/驱动资源竞争提出假设，并明确最终结论应以实验和 trace 为准。
- KV cache 大小估算：传统 MHA 近似为 `2 × L × B × S × H_kv × D_head × bytes`；GQA/MQA 中使用 KV head 数而不是 Q head 数。
- 拷贝何时值得：比较 NPU prefill 节省的时间、KV copy 与 context 切换成本、预计 decode 长度和 CPU/NPU decode 速度。

### 项目统一回答框架

任何项目都按下面 6 步回答，避免只罗列“做了什么”：

1. **场景与约束**：模型、硬件、prefill/decode、dtype、内存或正确性要求。
2. **现象与基线**：哪个指标慢，基线如何复现，性能口径是什么。
3. **归因与反证**：用了哪些 profile；提出过什么假设；如何排除错误方向。
4. **设计与实现**：改了哪层数据流、布局或调度；为什么理论上会更快；有哪些 guard/fallback。
5. **正确性与性能验证**：算子测试、PPL/logits、真实生成、端到端 benchmark；A/B 控制变量。
6. **结果、代价与下一步**：局部和端到端指标分开说，同时说明适用条件与副作用。

推荐句式：

> 我先看到的是 X 现象，但没有直接假设瓶颈在矩阵核；我用 A/B、trace 和硬件计数器把问题缩小到 Y，并用 Z 数据排除了原来的方案。最终修改了数据流中的 W，在正确性保持一致的前提下，局部指标提升 A、端到端提升 B。代价是 C，因此我保留了 D 回退路径。

### 与编译、算子优化、国产卡训练的连接

#### 编译方向

- 权重 prepack 对应常量权重的 AOT 变换和 layout specialization。
- Flash Attention 分块成本函数对应 schedule search、硬件 cost model 与 autotuning；合法性约束先裁剪候选，成本模型再做收益排序。
- attention/MoE chunk 解耦对应算子图调度、shape specialization 和 cost model。
- fast path guard/fallback 对应 pattern rewrite 的合法性条件与多版本代码生成。
- layerwise 加载和异构 prefill/decode 对应 device placement、buffer lifetime、跨设备 copy、异步依赖与内存规划。
- Expert 预测预取对应编译/运行时协同：静态图给出可重叠区间，动态 router/predictor 决定实际搬运对象。
- 需要补齐从 PyTorch/模型图到 MLIR/厂商 IR，再到 kernel/指令的 lowering 全链路。

#### 算子方向

面对任一算子，按以下顺序分析：

1. 明确 shape、dtype、layout、广播/归约维度和数值契约。
2. 计算 FLOPs、理论最少访存字节和 arithmetic intensity。
3. 判断瓶颈是 launch、memory、compute、同步还是 layout transform。
4. 再选择 fusion、tiling、vectorization、double buffering、数据重排、近似计算或专用矩阵单元。
5. 检查 occupancy/并行度、寄存器和片上存储压力、bank conflict、尾块与小 shape。
6. 用 reference + 多 shape/dtype 边界用例验证，用 warmup + 重复测量 + 端到端结果确认收益。

#### 国产卡训练方向

可迁移能力：算子语义、数据布局、异构后端、低精度、profiling、正确性闭环。

必须补齐的差异：

- 训练有 forward、backward、参数更新和随机性，需理解 autograd、梯度累积、checkpoint 和 optimizer state。
- 混合精度不仅是低比特推理，还涉及 FP16/BF16、FP32 master weight、loss scaling、溢出检测与归约精度。
- 多卡训练的核心瓶颈还包括 all-reduce、all-gather、reduce-scatter、all-to-all，以及 TP/PP/DP/SP/EP 的通信计算重叠。
- 训练性能要同时看 samples/tokens per second、MFU、显存、扩展效率、step time 分解和 loss 收敛一致性。
- 国产卡迁移还涉及算子覆盖、图编译失败、动态 shape、通信库、内存碎片和 fallback；不能只把 CUDA API 机械替换。

### 高频综合问题清单

1. 画出 `llama.cpp` 从计算图到 Hexagon 算子执行的完整链路。
2. HMX、HVX、Q6、VTCM、DMA 分别承担什么角色？
3. Q4_0 如何量化、存储、解量化，误差来自哪里？
4. 为什么权重布局转换适合前移？训练中为什么更难？
5. 如何用 Roofline 判断算子更应优化计算还是访存？
6. tile 大小如何受矩阵单元、向量宽度、寄存器、VTCM 和 occupancy 共同约束？
7. Flash Attention 为什么减少 HBM/DDR IO？online softmax 如何合并分块结果？
8. 分块成本函数应包含哪些项？如何证明它减少了真实硬件空闲而非只降低估算值？
9. 有限负 attention bias 与 `-inf` mask 的语义差异是什么？
10. MoE 的 router、top-K、capacity、overflow、expert dispatch 各是什么？
11. layerwise 加载如何计算峰值 working set？怎样安全复用异步设备 buffer？
12. 预取在什么条件下能用计算完全隐藏搬运？为什么 recall 不等于吞吐？
13. 为什么小 batch decode 很难打满矩阵单元？
14. 如何判断一次“算子提速”确实改善了端到端？
15. profile 会怎样扰动真实性能？异步阶段为什么不能直接相加？
16. 如何设计数值回归：reference、容差、PPL/logits 和真实生成分别解决什么问题？
17. 若一个优化局部快 2 倍、端到端只快 5%，如何解释并决定下一步？
18. 从单卡推理迁移到多卡训练，最需要新增哪些系统能力？

### 事实核对与防守边界

面试前为每个主项目准备一页“证据卡”，包括：代码文件和函数、修改前后伪代码、关键 shape/dtype/layout、运行命令、硬件与模型、基线与重复次数、误差阈值、失败方案、最终指标和已知限制。

以下表达应避免：

- 不说“做过国产卡训练”，应说“做过 Qualcomm HTP 推理后端，正在把方法迁移到国产卡训练知识体系”。
- 没有指令或计数器证据时，不说“完全打满 HMX”或“一个周期完成”。
- 不把局部 kernel/profile 收益说成端到端收益。
- 不把相关性预测说成保证命中，也不把 recall 直接等同于吞吐。
- 在没有公式和计数器证据前，不把成本模型估算的 idle time 说成硬件实测空闲周期。
- 不把 mmap、FastRPC 映射和设备侧 zero-copy 混为一谈；layerwise 中每一步是 copy 还是 map 必须按代码回答。
- 不背诵无法推导的 `248 FLOPs/byte`、tile 大小或 graph call 数；推不出来的数字面试前必须回到代码和日志核对。

### 本岗位的项目讲述顺序

1. 用 90 秒总述建立“端侧基模后端 + 算子 + 内存系统”的标签。
2. 用 DeepSeek 权重布局展示数据布局和编译期前移思维。
3. 用 Flash Attention 成本函数展示硬件感知的分块与调度；若公式和数据尚未核对，只做简述。
4. 用 Flash Attention mask 修复展示算子语义、数值分析与正确性工程。
5. 用 layerwise 加载和 Expert 预测预取连成“先解决容量、再隐藏搬运”的完整内存优化故事。
6. 面试官继续追问 profiling 时，再补充 GPT-OSS dispatch；追问异构执行时，再补充 NPU Prefill + CPU Decode。
7. 面试官转向训练时，先明确经验边界，再从 layout、低精度、内存生命周期、MoE 和 profiling 迁移到训练，并主动讨论 backward、混合精度和集合通信的差异。

## llvm编译器项目
### 面试准备
#### 前端部分
1. 词法分析 (Lexical) 与语法分析 (Syntax)
流程：parser/lexer.c 和语法解析规则（Flex/Bison 生成）。将文本流转换为 Token 流，然后依靠语法规则生成抽象语法树（AST）。
数据结构：在你的项目里，这对应 common/syntax_tree.h 和 common/ast.hpp。ast.hpp 定义了各种树节点，比如 BinaryExpr（加减乘除）、IfStmt（条件分支）、WhileStmt（循环）等。
1. AST 遍历与环境状态维护 (Visitor Pattern + Scope)
生成 IR 的核心机制是访问者模式（Visitor Pattern）。

在 sysy_builder.hpp 中，应该有一个构建器（Builder）类继承自 AST 的 Visitor。它重载了 visit(IfStmt* )、visit(BinaryExpr* ) 等方法。
它需要维护一个符号表（Symbol Table），记录当前作用域内的变量名到其分配的内存地址或 Value* 的映射。这样遇到 a = b + 1 里面的 b 时，能知道去哪个地址 Load 数据。
3. IRBuilder 与指令的生成（最关键的一步）
项目目录中有一个极其重要的文件：IRBuilder.hpp。它的作用就像是一个在流水线上的“打字机”。

维护插入点（Insert Point）：IRBuilder 内部保存了当前正在操作的 BasicBlock（基本块）。当你调用 builder->create_add(v1, v2) 时，它会自动生成一条对应的加法 Instruction，并将其 push_back 到当前基本块的指令链表（ilist）末尾。
4. 具体控制流结构如何生成？ (If / While)
这是面试常考的重点！如果在 AST 层遇到一个 while(exp) { body }，你的 Builder 会怎么生成 LightIR 的图？

生成新的基本块：用 BasicBlock::create(...) 创建至少 3 个基本块：cond_block（条件块）、body_block（循环体块）、next_block（退出循环的块）。
连接跳转指令（Branch）：
在进入 while 之前，生成一条无条件跳转指令跳转到 cond_block。
让 IRBuilder 的插入点移动到 cond_block，遍历生成 exp （条件表达式）的 IR。
根据 exp 的结果（Value*），利用 IRBuilder 生成一条条件跳转指令（Br cond_val, body_block, next_block）。
处理循环体：
将插入点移动到 body_block，递归调用 Visitor 处理循环体内部的 AST 节点。
循环体末尾，插入无条件跳转，跳回 cond_block。
结束点：将插入点移动到 next_block，继续处理后续的 AST。
#### 其余准备
【项目背景与整体负责模块】

“面试官您好，在这个 segfault-compiler 项目中，我参与实现了一个完整的、类 LLVM 架构的 SysY 语言编译器，能够将类 C 语言编译为 RISC-V/LoongArch 机器码。底层基于我们自己设计的 SSA 形式中间表示 LightIR。
在项目中，我端到端地打通了多项核心优化，主要负责了中端的过程间优化（函数内联）、循环优化（循环不变式外提 LICM）以及后端的线性扫描寄存器分配（Linear Scan）。我的目标是权衡编译耗时与最终生成的指令运行效率。”

【冲突与挑战：遇到了什么 Bug/瓶颈？怎么解决的？】

“在开发过程中，我遇到的**最大性能瓶颈和挑战来自于『函数内联与寄存器分配的连锁反应（相克）』**。

比如在起初做函数内联（Function Inlining）时，我采取了比较激进的策略。虽然这消除了函数调用的栈开销，但由于代码急剧膨胀（Code Bloat），导致了一个致命问题：变量的活跃区间（Live Interval）被拉得非常长，这使得同时活跃的变量数远远超过了物理寄存器的数量上限。
这直接压垮了我写的线性扫描寄存器分配器，导致后端生硬地把大量变量 Spill（溢出）到内存中。最终的测试结果是：激进内联后的代码，运行速度反而比没优化前还要慢，因为大量的访存指令吃掉了原本函数调用省下的时间。

【方案与结果：如何解决？】

为了解决这个问题，我做了两个改进：

在内联侧引入 Cost Model（开销成本模型）：我设计并实现了一套基于启发式规则的函数内联 Cost Model。该模型通过在编译前端动态测算目标函数（Callee）的指令规模作为惩罚，如果参数中有常量则作为奖励（可以引发常量传播优化），并设置内联深度限制，来评估内联的潜在收益与风险。只有当内联带来的性能提升明显超过代码膨胀引起的寄存器压力时，才会执行内联。

与此同时，我的函数内联把原本隐藏在函数调用内的代码平铺开后，极大地增加了**LICM（循环不变式外提）**的优化机会。内联暴露出的常数和循环内不变的地址计算，被 LICM 成功提到了循环前（Preheader）执行。最终这三个 Pass 形成了一个闭环，在编译时间和动态执行指令数上取得了很好的平衡，大幅提高了程序的性能水平。”

### 感悟
让我体会到编译优化是一门trade-off的艺术，过度优化可能适得其反。需要结合硬件与软件的特性，设计合理的规则来指导优化决策。

## mlir项目（fhe编译优化实现）
### 自举算子插入优化
基于动态规划算法优化自举算子的排布，性能相比传统手动插入提升20%。
- dp算法：这个 Pass 是我在基于 MLIR 构建的同态加密（HE）编译器中，为解决自举（Bootstrapping）操作最优插入问题而设计的核心优化模块。在同态加密计算中，自举操作耗时极高，同时长程数据依赖（Shortcuts）为了满足密文层级（Level）对齐的硬性要求，会强行压低沿途其余计算节点的可用层级，导致整体计算延迟剧增。为了最小化程序的端到端执行时间，我将该节点选址问题抽象为动态规划（DP）模型：用 DP 数组记录沿着计算图深度推进时，在每一层发生自举的最小累计延迟。在设计状态转移方程时，为了解决长程边带来的层级挤压矛盾，我引入了长程边子集枚举与代价评估策略，在‘保留长程边但牺牲同深度普通节点计算速度’与‘强制打断长程边增加单次自举开销以换取局部计算提速’之间进行精确的 Trade-off。最终，算法通过回溯 DP 路径，全局寻找到了自举开销与密集计算延迟之和最小的最优插入边界点与必须被阻断的长程边集合，并在 IR 层级为对应节点打上 Boot=true 标签，为后端代码生成提供了极具性能优势的自举调度策略。
> 算法详细拆解：
> 1. 明确 DP 状态定义与转移思路
“首先，我设定 DP 数组 dp[i] 来表示：当计算图推进到深度 i 并且恰好在第 i 层执行了一次自举操作时，整个程序端到端的最小累计延迟。
在计算 dp[i] 时，我们需要回头去寻找上一个最优的自举位置 j。那么状态转移方程的核心，就是如何精确计算在区间 [j, i] 之间不发生自举的期间，所产生的区间总延迟代价。”
>2. 解释引入难点：长程边（Shortcuts）与层级挤压矛盾
“计算区间代价时，最大的阻碍来自于跨越该区间的长程数据依赖（Shortcuts）。在同态加密的机制中，密文的每次乘法都会消耗可用的层级（Level），层级耗尽就必须做自举。
如果有一条长程边跨越了 [j, i] 这一长段区间，为了让这个数据能活着送达底层而不报错，在层级对齐机制下，它会强迫压低沿途经过的所有深度的可用层级。而同态加密中，节点在越低的层级下执行，耗时就越长。这就是所谓的‘层级挤压’导致普通节点变慢的问题。”
>3. 详细拆解 Trade-off（权衡决策）
“为了打破这个僵局，我们在处理每一条长程边时，其实都面临一个极端的 Trade-off：”
策略 A（保住长程边）：选择顺应长程边，不在起点 j 把它干掉。代价是：它会触发层级重新分配，拖慢区间内所有普通节点的计算速度。
策略 B（强制打断长程边）：选择在起点 j 强制让这条长程边也执行一次自举运算。代价是：承受一次巨大的固定自举延迟开销；收益是：消除了它对局部层级的束缚，区间内的普通节点可以分配到更高的层级，从而获得计算上的大幅提速。”
>4. 算法落地：子集枚举与精确代价评估
“由于现实计算图中，跨越某个区间的长程边往往不止一条，这是一个典型的组合优化子问题。因此我设计了长程边子集枚举机制：
对于当前评估的区间 [j, i]，我会先收集所有穿过它的长程边，然后枚举它们所有可能的保留组合（子集）。针对每一个子集，我都会进行一次模拟评估：
计算代价 = 『未能保留的长程边需要额外执行自举的开销总和』 + 『剩余节点在新的受限层级下的实际计算耗时总和』。
通过比较所有子集的结果，算法能动态地判断出：在当前的图结构和层深下，到底放弃哪些长程边、保留哪些长程边才是全局最划算的。最后取出这个最优子集的代价，作为 DP 状态转移的最优成本，从而彻底解决层级分配与开销最小化之间的矛盾。”

### 算子库优化


## cuda编程项目
针对 SNN 多时间步迭代导致访存密集的问题，独立设计并实现了一个基于 LeNet 架构的 CUDA SNN 前向推理引擎，支持 FashionMNIST 数据集的图像分类
### SNN介绍
与CNN不同，需要在时间维度上进行迭代，导致访存密集，性能较差，比如项目中需要循环8次

### GPU架构
GPU的核心是SM（Streaming Multiprocessor），每个SM包含多个CUDA核心，负责执行并行计算任务。
1. 逻辑结构：由grid，block和thread组成，线程是GPU执行的基本单位，线程块是线程的集合，网格是线程块的集合。
2. 内存层次：包括寄存器、共享内存、全局内存等，不同类型的内存具有不同的访问速度和范围。
3. 物理结构：GPU由多个SM组成，每个SM包含多个warp，每个warp中的（32个）线程同时执行相同的指令。
4. bank冲突：shared memory被划分为32个bank， 如果在$32 \times 32$ 的共享内存矩阵中，线程访问的地址在同一个bank中，就会发生bank冲突，导致性能下降。使用padding可以避免bank冲突。

### 项目细节
项目描述：使用 CUDA C++ 从头实现并优化了一个 SNN (脉冲神经网络) 的前向推理引擎，对 FashionMNIST 数据集进行分类。
网络架构：LeNet-5 变种 (Conv1 -> Pool1 -> Conv2 -> Pool2 -> FC1 -> FC2 -> FC3)。时间步长 T=8
核心优化点：
算子融合 (Kernel Fusion) / 持久化核函数 (Persistent Kernel)：没有调用 cuDNN 等库，而是直接手写了一个巨大的 persistent_infer_kernel，将所有的卷积、池化、全连接层以及 SNN 的时间循环 (T=8) 全部融合在一个 Kernel 中。避免了频繁的 Kernel launch 开销和中间变量在全局内存 (Global Memory) 中的反复读写。
细粒度并行映射：采用 Batch 并行策略，1 个 Block 处理 1 个样本 (blockIdx.x + batchOffset)。Block 内的 256 个 Threads 协同处理特征图和全连接层向量。
共享内存 (Shared Memory) 优化：使用 Shared Memory 协同加载整个输入图像 (28x28) 以及中间的 Pool1 结果。因为图像和特征图在卷积核滑动时会被重复读取，放进 Shared memory 极大降低了 Global Memory 带宽压力。
寄存器缓存与循环展开 (Register Caching & Unrolling)：在算子计算时，将小尺寸的卷积核窗口 (如 5x5 的 Conv1_K) 先手动提取入片上寄存器 (Register Array patch[CONV1_K * CONV1_K]) 再进行计算，配合 #pragma unroll 提升指令级并行度 (ILP)。
内联 PTX 汇编优化：通过手动编写内联汇编 ptx_fma_f32 强制发射 fma.rn.f32 (乘加融合指令)，最大化 ALU 计算吞吐量。
🤔 面试高频问题模拟 (Q&A)
1. 为什么采用一个大 Kernel (Persistent Kernel) 把所有层写在一起，而不是逐层 Launch Kernel？
回答要点：

消除 Launch Overhead：每次调用 Kernel 都有微秒级的开销。特别是在 SNN 中，计算包含一个时间循环 (T=8)，如果每层、每个时间步都 launch，相当于要几百次 launch，开销占比巨大。
数据局部性 (Data Locality)：融合在一块后，前一层的结果（或者说特征图甚至膜电位）可能还在 L1/L2 Cache 或者 Shared Memory 中，下一层直接读取处理，显著降低了昂贵的 Global Memory 访存开销。
1. 在这个 CUDA 程序里，你是如何使用 Shared Memory 优化的？
回答要点：

用途：主要用于缓存输入图像 (s_input) 和池化层1的输出 (s_pool1)。
原因：卷积操作具有极强的数据重用性 (Spatial Locality)。相邻的输出像素需要读取大面积重叠的输入像素。如果直接从 Global Memory 读，带宽会成为瓶颈。
实现：Block 启动后，首先让 256 个线程通过步进 (for (int i = tid; i < size; i += nthreads)) 协同搬运 Global Memory 的数据到 Shared Memory 中，然后调用 __syncthreads() 同步，最后各个计算线程都从 Shared memory 去取 patch 数据。
3. 你的 Block / Thread 并行划分策略是什么？有哪些利弊？
回答要点：

策略：采用 grid(bs), block(256)。即一个 Thread Block 负责计算 Batch 中的一张完整的图片，Block 内的 256 个 Thread 根据元素的全局索引 (idx = tid; idx < total; idx += nthreads) 分摊该层各个特征图/神经元的计算任务。
利 (优点)：易于共享该图片的中间数据（通过 Shared Memory 存中间特征图）。无需不同 Block 之间的同步，因为每个样本的推断是独立的。
弊 (潜在改进点)：256 个线程去平摊并行度，对于全连接层或较小特征图的情况可能导致资源利用不足。更大规模的网络可能需要 "把特征图拆分给多个 Block" 或者使用 Tensor Core。
4. 代码中看到了 ptx_fma_f32，为什么要手写 PTX 汇编指令？
回答要点：

FMA 表示 Fused Multiply-Add (d=a×b+c)。
在硬件底层，NVIDIA GPU 对乘加操作有专门的 FMA 指令执行单元。如果不加干预，编译器有时为了严格遵循浮点标准可能不会自动将乘法和加法折叠。
通过手动使用内联汇编强制发射 fma.rn.f32 指令，可以确保这两个运算在一个时钟周期内完成（并且只舍入一次提高精度），从而把计算逻辑的吞吐量压榨到极限。该手段常用于卷积、矩阵乘这类典型的计算密集型算子。
1. SNN (脉冲神经网络) 的计算和传统 CNN 在这段代码里的体现有什么区别？
回答要点：

网络增加了时间维度 for (int t = 0; t < T; ++t)。
增加了在时间维度上持久存在的膜电位 (Membrane Potential) 数组（如 d_conv1_pot）。
激活函数由 ReLU 变成了 LIF (Leaky Integrate-and-Fire) 模型的核心逻辑：即电压 V 累加输入后，判断 if (V > threshold)，如果超过阈值发出脉冲 (返回 1.0f 或者 boolean)，并将电压重置。脉冲(spike)会被作为下一层的输入传递。
