### cpp
1. 多态：
   1. 静态多态：函数重载、运算符重载、模板（泛型编程）
   2. 动态多态：虚函数、纯虚函数、抽象类
2. 如何避免内存泄漏？
   RAII（Resource Acquisition Is Initialization）原则：通过对象的生命周期管理资源，
   智能指针（std::unique_ptr, std::shared_ptr, std::weak_ptr）
   析构函数：确保在对象销毁时释放资源
